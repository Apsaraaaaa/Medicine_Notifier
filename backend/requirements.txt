Django==5.2.17
djangorestframework==3.18.0
djangorestframework-simplejwt==5.5.1
django-cors-headers==4.9.0
python-dotenv==1.2.3

# PostgreSQL driver (psycopg 3). The [binary] extra ships prebuilt wheels, so
# no local libpq or C compiler is needed.
psycopg[binary]==3.3.4

# --- Optional: image recognition for the prescription scanner ---------------
# The scanner works without these: /api/catalog/scan/ accepts the label text
# and parses it into form fields with no dependencies at all. Installing them
# — plus the Tesseract binary itself, which is not a pip package — additionally
# lets it read a photograph.
#
#   pip install pytesseract==0.3.13 Pillow==11.3.0
#   Windows: https://github.com/UB-Mannheim/tesseract/wiki  (add it to PATH)
#   Debian/Ubuntu: sudo apt install tesseract-ocr tesseract-ocr-nep
#
# GET /api/catalog/scan/ reports whether this server has an engine, and the app
# offers the camera or asks for typing accordingly.
